<UserControl x:Class="LogReader.App.Views.DashboardTreeView"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:LogReader.App.Controls"
             xmlns:vm="clr-namespace:LogReader.App.ViewModels"
             xmlns:model="clr-namespace:LogReader.Core.Models;assembly=LogReader.Core"
             Background="Transparent">
    <Border BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0">
        <Grid>
            <DockPanel x:Name="GroupsPanelContent" Margin="0,0,14,0">
                <DockPanel.Style>
                    <Style TargetType="DockPanel">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsGroupsPanelOpen}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DockPanel.Style>
                <StackPanel DockPanel.Dock="Top" Background="{StaticResource AppSurfaceBrush}">
                    <Grid Margin="8,6,8,2">
                        <TextBlock Text="Dashboards" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Margin="8,0,8,4">
                        <Button Command="{Binding CreateContainerGroupCommand}" IsEnabled="{Binding AreLoadAffectingActionsEnabled}" Style="{StaticResource ToolBarButton}" Padding="6,2" ToolTip="Create a new top-level folder">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B7;" Margin="0,0,4,0"/>
                                <TextBlock Text="+ Folder"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding CreateGroupCommand}" IsEnabled="{Binding AreLoadAffectingActionsEnabled}" Style="{StaticResource ToolBarButton}" Padding="6,2" ToolTip="Create a new top-level dashboard">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xF246;" Margin="0,0,4,0"/>
                                <TextBlock Text="+ Dashboard"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <Grid Margin="8,0,8,6" HorizontalAlignment="Stretch">
                        <Border CornerRadius="6" Background="{StaticResource AppElevatedSurfaceBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=DashboardFilterBox}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource AppFocusBorderBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <TextBox x:Name="DashboardFilterBox"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="10,3,8,3"
                                 VerticalContentAlignment="Center"
                                 Text="{Binding DashboardTreeFilter, UpdateSourceTrigger=PropertyChanged, Delay=200}"
                                 ToolTip="Filter dashboards/folders"/>
                        <TextBlock Text="Search..." Margin="11,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource AppPlaceholderTextBrush}" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=DashboardFilterBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    <StackPanel Margin="8,0,8,6" Visibility="{Binding IsDashboardLoading, Converter={StaticResource BoolToVis}}">
                        <ProgressBar IsIndeterminate="True" Height="3"/>
                        <TextBlock Text="{Binding DashboardLoadingStatusText}" Margin="0,4,0,0" FontSize="11" Foreground="{StaticResource AppMutedTextBrush}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </StackPanel>
                <Border DockPanel.Dock="Top"
                        BorderBrush="{StaticResource AppBorderBrush}"
                        BorderThickness="0,1,0,0"/>

                <Button DockPanel.Dock="Top"
                        Command="{Binding ShowAdHocTabsCommand}"
                        IsEnabled="{Binding AreLoadAffectingActionsEnabled}"
                        Background="Transparent"
                        BorderThickness="0"
                        Focusable="False"
                        HorizontalContentAlignment="Stretch"
                        Padding="0"
                        ToolTip="Show open tabs that are not assigned to any dashboard">
                    <Button.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Date Shift" SubmenuOpened="AdHocModifiers_SubmenuOpened" IsEnabled="{Binding AreLoadAffectingActionsEnabled}">
                                    <MenuItem Header="Loading..." IsEnabled="False"/>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="Clear Ad Hoc Files" Click="ClearAdHocFiles_Click" IsEnabled="{Binding AreLoadAffectingActionsEnabled}"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </ControlTemplate>
                    </Button.Template>
                    <Border BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAdHocScopeActive}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AppSelectedRowBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid x:Name="AdHocRowGrid" MinHeight="26" Cursor="Hand" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="18"/>
                                <ColumnDefinition Width="4"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       FontSize="9"
                                       MouseLeftButtonDown="AdHocExpand_MouseDown"
                                       Cursor="Hand"
                                       Foreground="{StaticResource AppDisclosureBrush}"
                                       Visibility="{Binding CanExpandAdHoc, Converter={StaticResource BoolToVis}}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="&#x25B6;"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAdHocExpanded}" Value="True">
                                                <Setter Property="Text" Value="&#x25BC;"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Border Grid.Column="1"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding AdHocScopeChipText}"
                                       VerticalAlignment="Center"
                                       FontSize="12"
                                       TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </Border>
                </Button>
                <ItemsControl DockPanel.Dock="Top"
                              ItemsSource="{Binding AdHocMemberFiles}"
                              Margin="22,0,0,4">
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsAdHocExpanded}" Value="True"/>
                                        <Condition Binding="{Binding CanExpandAdHoc}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:GroupFileMemberViewModel}">
                            <Border ContextMenuService.ShowOnDisabled="True">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{StaticResource AppSelectedMemberBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.ContextMenu>
                                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                        <MenuItem Header="Copy Full Path" Click="CopyFullPath_Click"/>
                                        <MenuItem Header="Open File Location" Click="OpenFileLocation_Click"/>
                                        <MenuItem Header="Close Tab" Click="CloseAdHocMemberFile_Click"/>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <controls:DashboardPathTextBlock Grid.Column="0"
                                                                     FileName="{Binding FileName}"
                                                                     FilePath="{Binding FilePath}"
                                                                     ShowFullPath="{Binding ShowFullPath}"
                                                                     Padding="4,2"
                                                                     FontSize="11"
                                                                     TextTrimming="CharacterEllipsis"
                                                                     Cursor="Hand"
                                                                     MouseLeftButtonUp="OpenAdHocMemberFile_Click"
                                                                     ToolTip="{Binding FilePath}">
                                        <controls:DashboardPathTextBlock.Style>
                                            <Style TargetType="{x:Type controls:DashboardPathTextBlock}">
                                                <Setter Property="Foreground" Value="{StaticResource AppMutedTextBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Foreground" Value="{StaticResource AppSelectedMemberTextBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </controls:DashboardPathTextBlock.Style>
                                    </controls:DashboardPathTextBlock>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding FileSizeText}"
                                               Padding="6,2,4,2"
                                               FontSize="10"
                                               Foreground="{StaticResource AppMutedTextBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding HasFileSize, Converter={StaticResource BoolToVis}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Border x:Name="AdHocSectionDivider"
                        DockPanel.Dock="Top"
                        Margin="0,0,0,4"
                        BorderBrush="{StaticResource AppBorderBrush}"
                        BorderThickness="0,1,0,0"/>

                <ListBox x:Name="GroupItemsControl"
                         ItemsSource="{Binding Groups}"
                         Background="Transparent"
                         BorderThickness="0"
                         AllowDrop="True"
                         DragOver="GroupTree_DragOver"
                         DragLeave="GroupTree_DragLeave"
                         Drop="GroupTree_Drop"
                         ScrollViewer.CanContentScroll="True"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         VirtualizingPanel.ScrollUnit="Pixel">
                    <ListBox.ContextMenu>
                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                            <MenuItem Header="Expand All Folders" Command="{Binding ExpandAllFoldersCommand}"/>
                            <MenuItem Header="Collapse All Folders" Command="{Binding CollapseAllFoldersCommand}"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:LogGroupViewModel}">
                            <Border BorderBrush="{StaticResource AppDividerBrush}" BorderThickness="0,0,0,1" Visibility="{Binding IsTreeVisible, Converter={StaticResource BoolToVis}}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Kind}" Value="{x:Static model:LogGroupKind.Branch}">
                                                <Setter Property="Background" Value="{StaticResource AppBranchRowBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{StaticResource AppSelectedRowBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <Grid>
                                        <ItemsControl ItemsSource="{Binding GuideRailMargins}" IsHitTestVisible="False">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <Grid/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="1"
                                                            Margin="{Binding}"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Stretch"
                                                            Background="{StaticResource AppBorderBrush}"
                                                            Opacity="0.7"
                                                            SnapsToDevicePixels="True"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <Grid x:Name="GroupRowGrid" Margin="{Binding RowIndentMargin}" Background="Transparent" MouseLeftButtonDown="GroupRow_MouseDown" MouseLeftButtonUp="GroupRow_MouseUp" PreviewMouseMove="GroupRow_PreviewMouseMove" Cursor="Hand" ToolTip="Click to open dashboard files. Row buttons create items here.">
                                            <Grid.ContextMenu>
                                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                                    <MenuItem Header="Date Shift" SubmenuOpened="DashboardModifiers_SubmenuOpened" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}">
                                                        <MenuItem Header="Loading..." IsEnabled="False"/>
                                                    </MenuItem>
                                                    <Separator Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}"/>
                                                    <MenuItem Header="New Folder Here" Command="{Binding DataContext.CreateChildFolderCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="{Binding CanAddChild, Converter={StaticResource BoolToVis}}"/>
                                                    <MenuItem Header="New Dashboard Here" Command="{Binding DataContext.CreateChildDashboardCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="{Binding CanAddChild, Converter={StaticResource BoolToVis}}"/>
                                                    <MenuItem Header="Add Files..." Command="{Binding DataContext.AddDashboardFilesCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}"/>
                                                    <MenuItem Header="Bulk Open Files..." Command="{Binding DataContext.BulkAddDashboardFilesCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Rename" Click="RenameGroup_Click" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Move Up" Command="{Binding DataContext.MoveDashboardGroupUpCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}"/>
                                                    <MenuItem Header="Move Down" Command="{Binding DataContext.MoveDashboardGroupDownCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Expand All Folders" Command="{Binding DataContext.ExpandAllFoldersCommand, Source={x:Reference Root}}"/>
                                                    <MenuItem Header="Collapse All Folders" Command="{Binding DataContext.CollapseAllFoldersCommand, Source={x:Reference Root}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Reload Dashboard" Click="ReloadDashboard_Click" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="Delete Item"
                                                              Command="{Binding DataContext.DeleteGroupCommand, Source={x:Reference Root}}"
                                                              CommandParameter="{Binding}"
                                                              IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}"
                                                              Foreground="{StaticResource AppErrorBrush}"/>
                                                </ContextMenu>
                                            </Grid.ContextMenu>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="18"/>
                                                <ColumnDefinition Width="4"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="9" MouseLeftButtonDown="GroupExpand_MouseDown" Cursor="Hand" Foreground="{StaticResource AppDisclosureBrush}" Visibility="{Binding CanExpand, Converter={StaticResource BoolToVis}}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="&#x25B6;"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                <Setter Property="Text" Value="&#x25BC;"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <Border Grid.Column="1"/>
                                            <Grid Grid.Column="2">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Margin="0,0,4,0"
                                                           VerticalAlignment="Center"
                                                           FontSize="12"
                                                           Foreground="{StaticResource AppErrorBrush}"
                                                           Text="{Binding ErrorCountTag}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding HasErroredMemberFiles}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" FontSize="12" TextTrimming="CharacterEllipsis">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="FontWeight" Value="Normal"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Kind}" Value="{x:Static model:LogGroupKind.Branch}">
                                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                            <TextBox Grid.Column="2" Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" FontSize="12" KeyDown="GroupNameTextBox_KeyDown" LostFocus="GroupNameTextBox_LostFocus" IsVisibleChanged="GroupNameTextBox_IsVisibleChanged">
                                                <TextBox.Style>
                                                    <Style TargetType="TextBox">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBox.Style>
                                            </TextBox>
                                            <Border Grid.Column="3"
                                                    HorizontalAlignment="Right">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Width" Value="98"/>
                                                        <Setter Property="Opacity" Value="0"/>
                                                        <Setter Property="IsHitTestVisible" Value="False"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Kind}" Value="{x:Static model:LogGroupKind.Branch}">
                                                                <Setter Property="Width" Value="74"/>
                                                            </DataTrigger>
                                                            <MultiDataTrigger>
                                                                <MultiDataTrigger.Conditions>
                                                                    <Condition Binding="{Binding IsMouseOver, ElementName=GroupRowGrid}" Value="True"/>
                                                                    <Condition Binding="{Binding IsEditing}" Value="False"/>
                                                                </MultiDataTrigger.Conditions>
                                                                <Setter Property="Opacity" Value="1"/>
                                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                                            </MultiDataTrigger>
                                                            <MultiDataTrigger>
                                                                <MultiDataTrigger.Conditions>
                                                                    <Condition Binding="{Binding IsSelected}" Value="True"/>
                                                                    <Condition Binding="{Binding IsEditing}" Value="False"/>
                                                                </MultiDataTrigger.Conditions>
                                                                <Setter Property="Opacity" Value="1"/>
                                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                                            </MultiDataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <StackPanel Orientation="Horizontal"
                                                            HorizontalAlignment="Right">
                                                    <Button Content="&#xE70F;"
                                                            FontFamily="Segoe MDL2 Assets"
                                                            FontSize="12"
                                                            Padding="3,1"
                                                            Margin="2,0,0,0"
                                                            IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}"
                                                            Click="RenameGroup_Click">
                                                        <Button.Style>
                                                            <Style TargetType="Button" BasedOn="{StaticResource ToolBarButton}">
                                                                <Setter Property="ToolTip" Value="Rename dashboard"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Kind}" Value="{x:Static model:LogGroupKind.Branch}">
                                                                        <Setter Property="ToolTip" Value="Rename folder"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                    <Button Content="&#xE8B7;" FontFamily="Segoe MDL2 Assets" FontSize="12" Padding="3,1" Margin="2,0,0,0" Command="{Binding DataContext.CreateChildFolderCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Style="{StaticResource ToolBarButton}" ToolTip="Create a new folder here" Visibility="{Binding CanAddChild, Converter={StaticResource BoolToVis}}"/>
                                                    <Button Content="&#xF246;" FontFamily="Segoe MDL2 Assets" FontSize="12" Padding="3,1" Margin="2,0,0,0" Command="{Binding DataContext.CreateChildDashboardCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Style="{StaticResource ToolBarButton}" ToolTip="Create a new dashboard here" Visibility="{Binding CanAddChild, Converter={StaticResource BoolToVis}}"/>
                                                    <Button Content="&#x25B2;" FontSize="9" Padding="3,1" Margin="2,0,0,0" Command="{Binding DataContext.MoveDashboardGroupUpCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="Collapsed" Style="{StaticResource ToolBarButton}" ToolTip="Move up"/>
                                                    <Button Content="&#x25BC;" FontSize="9" Padding="3,1" Margin="2,0,0,0" Command="{Binding DataContext.MoveDashboardGroupDownCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Visibility="Collapsed" Style="{StaticResource ToolBarButton}" ToolTip="Move down"/>
                                                    <Button Content="&#x1F4C4;+" FontSize="11" Padding="4,2" Margin="2,0,0,0" Command="{Binding DataContext.AddDashboardFilesCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Style="{StaticResource ToolBarButton}" ToolTip="Add files" Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}"/>
                                                    <Button FontSize="11" Padding="4,2" Margin="2,0,0,0" Command="{Binding DataContext.BulkAddDashboardFilesCommand, Source={x:Reference Root}}" CommandParameter="{Binding}" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}" Style="{StaticResource ToolBarButton}" ToolTip="Bulk add files" Visibility="{Binding CanManageFiles, Converter={StaticResource BoolToVis}}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="&#x1F4C4;" Margin="0,0,-3,0"/>
                                                            <TextBlock Text="&#x1F4C4;"/>
                                                            <TextBlock Text="+" Margin="1,0,0,0"/>
                                                        </StackPanel>
                                                    </Button>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                    <Grid>
                                        <ItemsControl ItemsSource="{Binding GuideRailMargins}" IsHitTestVisible="False">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <Grid/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="1"
                                                            Margin="{Binding}"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Stretch"
                                                            Background="{StaticResource AppBorderBrush}"
                                                            Opacity="0.7"
                                                            SnapsToDevicePixels="True"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <ListBox ItemsSource="{Binding MemberFiles}"
                                             Margin="{Binding MemberFilesMargin}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             AllowDrop="True"
                                             DragOver="MemberFileList_DragOver"
                                             DragLeave="MemberFileList_DragLeave"
                                             Drop="MemberFileList_Drop"
                                             ScrollViewer.CanContentScroll="True"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                             VirtualizingPanel.IsVirtualizing="True"
                                             VirtualizingPanel.VirtualizationMode="Recycling"
                                             VirtualizingPanel.ScrollUnit="Pixel">
                                        <ListBox.Style>
                                            <Style TargetType="ListBox">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListBox.Style>
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="Margin" Value="0"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Focusable" Value="False"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <ContentPresenter/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:GroupFileMemberViewModel}">
                                                <Border Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                        PreviewMouseLeftButtonDown="MemberFileRow_PreviewMouseLeftButtonDown"
                                                        PreviewMouseMove="MemberFileRow_PreviewMouseMove">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="Transparent"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                    <Setter Property="Background" Value="{StaticResource AppSelectedMemberBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.ContextMenu>
                                                        <ContextMenu>
                                                            <MenuItem Header="Copy Full Path" Click="CopyFullPath_Click"/>
                                                            <MenuItem Header="Open File Location" Click="OpenFileLocation_Click"/>
                                                            <Separator/>
                                                            <MenuItem Header="Remove from Dashboard" Click="RemoveDashboardFile_Click" IsEnabled="{Binding DataContext.AreLoadAffectingActionsEnabled, Source={x:Reference Root}}"/>
                                                        </ContextMenu>
                                                    </Border.ContextMenu>
                                                    <StackPanel Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <controls:DashboardPathTextBlock Grid.Column="0" FileName="{Binding FileName}" FilePath="{Binding FilePath}" ShowFullPath="{Binding ShowFullPath}" Padding="4,2" FontSize="11" TextTrimming="CharacterEllipsis" Cursor="Hand" Tag="{Binding Tag, RelativeSource={RelativeSource AncestorType=StackPanel}}" MouseLeftButtonUp="OpenMemberFile_Click" ToolTip="{Binding FilePath}">
                                                                <controls:DashboardPathTextBlock.Style>
                                                                    <Style TargetType="{x:Type controls:DashboardPathTextBlock}">
                                                                        <Setter Property="Foreground" Value="{StaticResource AppMutedTextBrush}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                                <Setter Property="Foreground" Value="{StaticResource AppSelectedMemberTextBrush}"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                                                                <Setter Property="Foreground" Value="{StaticResource AppErrorBrush}"/>
                                                                                <Setter Property="FontStyle" Value="Italic"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </controls:DashboardPathTextBlock.Style>
                                                            </controls:DashboardPathTextBlock>
                                                            <TextBlock Grid.Column="1" Text="{Binding FileSizeText}" Padding="6,2,4,2" FontSize="10" Foreground="{StaticResource AppMutedTextBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Visibility="{Binding HasFileSize, Converter={StaticResource BoolToVis}}"/>
                                                        </Grid>
                                                        <TextBlock Text="{Binding ErrorMessage}" Padding="4,0,4,2" FontSize="10" Foreground="{StaticResource AppErrorBrush}" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
            <Button x:Name="GroupsPanelRailButton" Command="{Binding ToggleGroupsPanelCommand}" ToolTip="Open dashboards pane" Background="{StaticResource AppSurfaceBrush}" BorderThickness="0" Padding="4,8">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsGroupsPanelOpen}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="&#x25B6;" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="14" Text="&#xECA5;" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button Width="14" HorizontalAlignment="Right" VerticalAlignment="Stretch" Command="{Binding ToggleGroupsPanelCommand}" ToolTip="Collapse dashboards pane" Background="{StaticResource AppSurfaceBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1,0,0,0" Padding="0" Content="&#x25C0;">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsGroupsPanelOpen}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>
