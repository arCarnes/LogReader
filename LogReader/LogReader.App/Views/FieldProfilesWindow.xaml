<Window x:Class="LogReader.App.Views.FieldProfilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LogReader.App.ViewModels"
        Title="Field profiles" Width="980" Height="760" MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,10">
            <Button DockPanel.Dock="Right" Content="Remove profile" Command="{Binding RemoveProfileCommand}" Padding="8,4" Margin="8,0,0,0"/>
            <Button DockPanel.Dock="Right" Content="Add profile" Command="{Binding AddProfileCommand}" Padding="8,4" Margin="8,0,0,0"/>
            <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name" MinWidth="200"/>
        </DockPanel>
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBox Text="{Binding SelectedProfile.Name, UpdateSourceTrigger=PropertyChanged}" ToolTip="Profile name" Padding="4"/>
            <TextBlock Text="Each regex requires a capture matching its field name, e.g. duration=(?&lt;duration_ms&gt;\S+). First match and first capture win."
                       TextWrapping="Wrap" Margin="0,6,0,0"/>
        </StackPanel>
        <DataGrid x:Name="FieldsGrid" Grid.Row="2" ItemsSource="{Binding SelectedProfile.Fields}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Field name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="145"/>
                <DataGridComboBoxColumn Header="Type" ItemsSource="{x:Static vm:FieldDefinitionEditorViewModel.Types}" SelectedItemBinding="{Binding Type}" Width="95"/>
                <DataGridTextColumn Header="Extraction regex" Binding="{Binding Pattern, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridCheckBoxColumn Header="Match case" Binding="{Binding CaseSensitive}" Width="95"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8">
            <Button Content="Add field" Command="{Binding AddFieldCommand}" Padding="8,4"/>
            <Button Content="Remove field" Command="{Binding RemoveFieldCommand}" CommandParameter="{Binding SelectedItem, ElementName=FieldsGrid}" Padding="8,4" Margin="8,0"/>
            <TextBlock Text="Sample lines (not saved; preview reads the first 100 lines):" VerticalAlignment="Center"/>
        </StackPanel>
        <TextBox Grid.Row="4" Text="{Binding SampleText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True"
                 MaxLength="65536" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,8">
            <Button Content="Preview" Command="{Binding PreviewCommand}" Padding="10,4"/>
            <Button Content="Cancel preview" Command="{Binding PreviewCancelCommand}" Padding="10,4" Margin="8,0"/>
        </StackPanel>
        <DataGrid Grid.Row="6" ItemsSource="{Binding PreviewRows}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="60"/>
                <DataGridTextColumn Header="Extracted fields" Binding="{Binding FieldsText}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="7" Margin="0,10,0,0">
            <Button DockPanel.Dock="Right" Content="Cancel" IsCancel="True" Width="80" Padding="4,6" Margin="8,0,0,0"/>
            <Button DockPanel.Dock="Right" Content="OK" Click="OK_Click" Width="80" Padding="4,6"/>
            <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Window>
